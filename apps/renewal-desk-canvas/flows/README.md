Populated starting with notebook 29 (`pac solution unpack` output — cloud
flows land here as per-flow JSON, one file per flow, under this directory
rather than a `modernflows/` subfolder of a full solution unpack, to keep
this app's flows reviewable independent of the agent fleet's solution).
Empty at repo init — see ../CHANGELOG.md.
