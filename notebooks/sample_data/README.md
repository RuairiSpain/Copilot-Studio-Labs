# sample_data/

Placeholder inputs the notebooks reference — none are committed as binary
files here since they're workshop-specific (real or realistic-fake supplier
documents). Generate or source your own before running the notebook that
needs them:

| File | Used by | Notes |
|---|---|---|
| `meridian-msa.pdf` | `03` | A short, fake MSA with a notice period, pricing terms, termination clause — the fixture the whole Knowledge track builds on. |
| `northwind-pricing-addendum-scan.pdf` | `04` | Must be a **scanned image**, not a text PDF — the point of `04` is exercising OCR via the AI Search skillset. |
| `capstone-corpus/*.pdf` | `15` | Any small multi-document corpus distinct from `03`-`06`'s sources, so the serverless-vs-always-on comparison isn't confounded by source overlap. |
| `poisoned-news-page.html` | `19` | Generated by the notebook itself — not a pre-existing fixture. |

Keep anything you add here fictional. This is a public curriculum repo.
